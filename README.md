# Campus Lunch Pipeline
Automated pipeline that fetches SharePoint-hosted menu PDFs via Playwright, parses them with Claude, stores results in PostgreSQL, and delivers daily Discord notifications via n8n.
